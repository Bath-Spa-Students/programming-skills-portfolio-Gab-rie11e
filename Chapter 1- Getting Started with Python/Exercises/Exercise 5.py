userRadius = float(input("Please enter radius:\n"))
print(userRadius)

pi = 3.14

# (A = π r²)

# (outputArea = π userRaidus²)

sqRadius = userRadius * userRadius

outputArea = pi * sqRadius

print(outputArea)